begin
  prog = irexec
  button = KEY_MEDIA
  config = "/home/builder/.config/i3/scripts/starter.sh google-chrome"
end

begin
  prog = irexec
  button = KEY_HELP
  config = "/home/builder/.config/i3/scripts/starter.sh kodi"
end

begin
  prog = irexec
  button = KEY_TEXT
  config = "/home/builder/.config/i3/scripts/starter.sh vdr_userspace"
end
